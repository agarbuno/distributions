if (interactive()) {
  suppressMessages(require(devtools))
}

options(
  usethis.full_name = "Alfredo Garbuno",
  usethis.protocol = "ssh",
  usethis.description = list(
    "Authors@R" = utils::person(
      "Alfredo", "Garbuno",
      email = "alfredo.garbuno@itam.mx",
      role = c("aut", "cre")
    ),
    Version = "0.0.0.9000"
  ),
  usethis.overwrite = TRUE
)
